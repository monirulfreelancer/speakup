node_modules
.next
.git
.env
.env.*
!.env.example
src/generated
*.md
!README.md
.DS_Store
Dockerfile
.dockerignore
docker-compose.yml

# The realtime service is its own deployable with its own toolchain —
# the web image must never contain or compile it.
realtime
